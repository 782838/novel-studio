# 更新日志

所有重要变更都会记录在这里。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## v1.4.3（2026-09-18，待发布·本地测试版）

### 稳定性 · 智谱清言免费模型「一直挂着等，直到成功或手动停止」

- **根本修复「几秒就放弃 / 像卡死」**：之前单次请求只等 3 分钟就被掐断，免费模型排队 / 慢生成要十几分钟才出结果，于是「连上→被掐→重新排队→再被掐」永远轮不到；退避又封顶 30 秒，看着像卡死。现在：
  - **单次请求给足 30 分钟**：连接成功但模型还在排队 / 慢慢生成，就一直等（像智谱网页那样一直转圈），不再中途掐断。
  - **失败密集重试、不停**：遇到 429（模型当前问题过大）、500/502/503/504、网络连接失败 / 超时，全部**一直重试**，起始 2 秒、封顶 10 秒，不成功就一直发请求。
  - **点「停止」才停**：只有你手动点停止才中止；非限流错误（401/400/403/404 等真错误）仍立即报错，不会傻等。
- **「测试连接」保持快速失败**：设置里的测试连接按钮只重试 1 次就返回，避免测连不通时卡十几分钟。
- **所有 AI 功能统一接入「等待浮层 + 可停止」**：之前只有对话框有「重试中」提示和停止键，记忆点 / 续写 / 大纲 / 角色 / 线路 / 设定 / 脑暴生成在模型繁忙时虽然也在重试，但界面看不到状态、也停不下来。现在**全部**统一：发起 AI 请求时弹出「已等待 N 秒 · 模型繁忙时会自动重试」浮层并带「停止」按钮——点停止即中断本次（含正在重试中的）请求；续写、记忆点批量生成也会把停止信号透传给后端，真正中断而不是等这一章跑完。

### 体验 · 统一「置顶」与「已完成」

- **项目备忘 / 线路 / 角色集 / 设定 新增「已完成」按钮**（点击后变灰沉到列表末尾，代表「已用上、用不上了」）：
  - 备忘：卡片右上角 ✓ 标记，已完成的排到最后。
  - 线路：每条泳道头部「标记完成」，已完成泳道变虚线灰边、沉底。
  - 角色集：角色卡 ✓ 标记，已完成角色变灰沉底。
  - 设定：每张设定卡新增「标记完成」，已完成卡片变灰沉底。
- **设定 补齐「置顶」**：设定此前既无置顶也无已完成，本次一并补上 📌 置顶与 ✓ 已完成。
- **伏笔 补齐「置顶」**：伏笔此前只有「已回收」，本次补上 📌 置顶——置顶的伏笔固定排在最前，回收状态与排序不受影响。
- **按钮不再"隐形"**：角色卡、备忘的置顶 / 已完成过去只有极小的半透明图标，几乎看不出是可点的按钮。现在统一改成**带文字的实体按钮**（「📌 置顶 / 已置顶」「标记完成 / ✓ 已完成」），常态可见、带边框，一眼就知道能点；角色卡的这组按钮也从右上角悬浮改为卡片内单独一行，不会再压到角色名。
- **排序规则统一**：置顶优先 → 已完成沉底 → 最近修改排前；置顶的条目即使标记已完成也仍固定在最前（是你主动选择）。

### 体验 · 所有 AI 功能都接进 AI 助手

- **点任何 AI 按钮，都会自动打开 AI 助手，像跟助手对话一样把过程演给你看**：生成记忆点（单章 / 全部生成）、续写、生成大纲、生成角色、梳理线路、补全设定、脑暴，现在统一走助手面板——自动展开、实时显示「正在生成第几章 / 共几章」「已生成 N 条候选」「正与模型通信中…」等状态，结束时在末尾汇报「已完成：成功 N 章」或「已完成：生成 N 条候选，写入项目 K 项」。
- **过程可停止**：每张任务卡都带「停止」按钮和「已等待 N 秒」计时，点停止即中断本次请求（信号一路透传到后端），不用干等。
- **任务与对话合流**：这些执行记录按时间并入助手对话流一起显示，但它们不是聊天消息、不写进历史，清空对话时一并清掉，不会污染你和助手的对话。
- 记忆点批量生成去掉了页面内的进度条，进度与结果统一在助手面板里看。

### 修复

- **思考过程一滚动就被弹回顶部**：AI 助手流式回答时，每次来新内容都会整体重绘消息区，把「思考过程」框的滚动位置重置掉——你往下翻，马上被弹回最上面。现在重绘前会先记录、重绘后按块还原每个思考块的滚动位置与展开状态；主消息列表在你手动上翻时也不会再被拽回顶部。
- **备忘卡的「置顶 / 已完成」两个按钮叠在一起**：备忘卡复用了 `.icon-btn`——那是给弹窗关闭、面板工具条等**纯图标**按钮准备的 26×26 方格，改成带文字后文字溢出格子，两个按钮互相压住，看上去像一团乱码。现在这组按钮的固定宽高与网格布局一并覆盖成自适应尺寸，两个按钮并排显示、互不重叠；卡片头部同时允许换行，窄窗口下也不会被挤变形。

## v1.4.2（2026-09-18）

### 稳定性

- **智谱清言免费模型「连接繁忙」自动重试**：免费模型有时会返回 429（模型当前问题过大，请稍后再试）。现在遇到这类限流会**自动重试最多 3 次**，采用指数退避（约 1s → 2s → 4s），不再需要你手动点「重试」就能连上；只有非限流错误才立即报错。流式 / 非流式调用都已覆盖，控制台会打印重试日志方便排查。

### 防误触

- **清空记忆点增加二次确认**：章节记忆点的「清空」按钮现在会先弹出确认框，显示要清空的章节名并提示「章节正文不受影响」，点「取消」数据不动。配合此前已加确认的备忘 / 伏笔 / 设定删除，所有会清空数据的操作现在都有保护。

## v1.4.1（2026-09-18）

### 版本更新检测

- **每次打开软件自动检测新版本**：启动后台静默查一次 GitHub 最新发布，发现新版时右上角按钮点亮成「⬆ 新版本 x.y.z」，并弹一条提示——不打断你写作。
- **顶栏新增「检测更新」按钮**：随时手动检测（按钮提示里能看到当前版本）。有新版本时打开详情页：当前版本 / 最新版本 / 发布时间 / 安装包名 / 本次更新说明。
- **下载与忽略**：详情里「前往下载」直接用系统浏览器打开安装包地址；不想升级可以「跳过此版本」，之后启动不再重复提示（手动检测仍能看到）。
- 断网、超时都会给出友好提示；自动检测失败完全静默，不会打扰。新增 `/api/meta` 返回 `version`。

### 新增

- **AI 记忆点**（侧边栏「章节」下方）：给每一章生成一小段**精简剧情摘要**，把整本书浓缩成「几百字就能读完」的进度速览，让 AI 用最少的字掌握「讲了什么、讲了多少、讲到哪」。
  - **一键生成**：单章点「✨ 生成」，或「生成缺失的记忆点」批量补齐——带进度条、可随时「停止」；未配置模型时也会用本地压缩先出结果，配置后质量更好。
  - **逐章可编辑**：每条记忆点都能手动修改或清空；顶部实时显示「章节总数 / 已生成 / 正文总字数 / 记忆点字数（压缩比）」。
  - **喂给助手**：助手读取项目上下文时，会看到「剧情记忆点（全书进度速览）」以及每章的记忆点，**不必再逐章读原文**就能判断剧情；只有需要细节或续写某一章时才去读正文，省下大量 token。
  - **导出全书记忆为 TXT**：一键把全书记忆点导出成 `.txt` 文件（带 UTF-8 BOM，Windows 记事本打开中文不乱码），可在系统弹窗里选保存位置；原来的「复制到剪贴板」作为兜底保留。
  - **导入 TXT**：把现成的记忆点文本直接导入，四种写法都认：`第3章 …`、`3. …`、`标题：…`，以及没有任何标记的纯顺序文本（按顺序补进剩下的章节）。导入前会先弹出预览——可直接改文字、逐条勾选，**原本已有记忆点的默认不勾选**，避免误覆盖；UTF-8 / GBK 编码都能正确读取。

### 角色集与关系图

- **角色置顶**：每张角色卡右上角有 📌，一键置顶 / 取消；置顶角色始终排在最前（卡片带高亮描边），也支持在角色档案里切换。
- **关系图重构为「聚焦式」**：打开关系图后，**点击任意角色**，其余人物淡出，只亮出他的关系网；右侧同步出现「关系网」侧栏——列出他所有的联系人、方向（→ 他给予 / ← 别人给予）、关系标签，以及每条关系是「稳定」还是「可能变化」。点「✕ 查看全部」恢复全景。
- **关系可以标记「可能变化」**：添加 / 编辑关系时可选「稳定 / 可能变化」，并写下变化方向（如「表面盟友→最终反目」）。这类关系在图上是**琥珀色虚线**，在列表里带「可能变化」徽标和「走向：…」提示——一眼看出哪些人物关系是随时会引爆的伏笔。
- 角色档案新增「🕸 关系网」按钮，一键跳转到他在关系图中的聚焦视图；关系列表每条都可以直接编辑。

### AI 助手交互

- **Enter 直接发送**（不再需要 Ctrl + Enter）；`Shift + Enter` 换行。使用中文输入法选词时的回车不会被误判为发送。

### 体验修复（同日追加）

- **角色档案按钮不再挤成一行**：底部按钮栏支持自动换行，五个功能按钮各得其所。
- **线路支持 📌 置顶**：每条泳道头部新增置顶按钮，置顶线路排到最前（如把感情线钉在主线上方便对照）。
- **伏笔分组排序**：**未回收的始终排最前**；勾选「已回收」的进入下方已回收区，且**越晚勾选越靠前**，刚回收完一眼就能复核。
- **统一「最近修改排最前」**：备忘、伏笔、设定、角色集、线路五处的默认排序全部改为**按最近修改时间倒序**——刚编辑过的内容自动浮到最上面，不用往下翻找；置顶的角色 / 线路 / 备忘仍固定在最前，不受影响。
- **角色档案不再挤成一团**：左侧属性栏此前被右侧关系表压成一条窄缝，长文案折成竖排、后面的字段全被挤没——现在两栏各自撑开，属性列通栏显示；**长内容默认只显示两行**，点「展开全文 · N 字」看完整内容（展开视图里也能直接进编辑），弹窗底栏的「编辑」保留为**总编辑**入口，一次改完整张角色卡。
- **导出人物卡（TXT）**：角色集工具栏新增「📄 导出人物卡」，可**一次勾选多个角色**一起导出（默认全选，支持全选 / 全不选），角色档案里也能单独导出当前角色。文件内含八项设定 + 人物关系（带「可能变化」标注与走向）+ 出现在的线路，**不同角色之间空出一排**便于阅读，带 UTF-8 BOM 保证记事本打开中文不乱码。
- **修复「左侧章节列表莫名跳回第一章」——同类问题一次性修完**：新建章节、把章节状态从草稿改成完成、点章节、AI 插入正文之后，列表都会跳回最上面。根因是视图整块重建时，浏览器把里面**所有**滚动容器都重置到顶部（之前的修法只处理了「点章节」一条路径）。现在改成在重绘前后统一记录并恢复滚动位置，**对所有视图的滚动区域都生效**；新建的章节还会自动滚进视野，不用自己去找。
- **顺带修掉一个更隐蔽的数据问题**：打字后在 0.7 秒防抖窗口内去改状态 / 新建 / 切视图，**最后一段输入会丢**（界面已重绘、内容没写回）。现在任何重绘之前都会先把编辑框里没落盘的输入写回，并且**等写完再拉新数据**，不会再丢字或读到旧值。
- **章节编辑器新增「⛶ 全屏」写作**：一键进入沉浸写作——顶栏、侧边栏、章节列表、工具条统统让位，编辑器铺满整窗；正文收成**居中的窄栏**（满屏一行太宽反而不利于阅读），顶部保留当前章节名、实时字数与退出按钮，正文照常编辑、自动保存不中断。`Esc` 一键退出，按钮状态同步。
- **「出现在的线路」也照人物关系改了**：长说明默认截断成一行（带「点开看全部」），**点整行**打开节拍详情——所属线路与类型、第几拍、状态、关联大纲节点、完整说明；详情里可直接「编辑」这个节拍，保存后档案自动刷新。
- **角色档案右侧「人物关系」也能点开看全部**：长说明默认截断成一行（看不完的会标「点开看全部」），**点整行打开关系详情**——对象、方向、稳定 / 可能变化、变化走向、完整说明一次看全；详情里可直接「编辑 / 删除」。「编辑 / 删除」也从原来淡淡的图标换成了清楚的文字按钮，删除同样要二次确认。档案弹窗加宽，左右两栏都不再挤。
- **补齐角色集的排序**：上一批「最近修改排最前」漏掉了角色集，现已补上（置顶角色仍固定在最前）。
- **删除有确认，不再怕误触**：备忘、伏笔、世界设定的「删除」现在都会先弹确认框（显示条目名），点「取消」即返回，数据不动。
- **备忘只显示简要**：卡片正文最多 3 行截断，列表更整洁；新增「展开全文」查看完整内容，查看弹窗里可直接转「编辑」——不再一点卡片就进编辑。
- **修复章节点击跳回顶部**：在章节列表里点了哪章，页面不再弹回最上面（重绘后恢复滚动位置）；切换前还会把正文框里**还没来得及自动保存的最后几百毫秒输入**先落盘，不再丢字。

### 安装包

- 安装界面新增「是否在桌面创建快捷方式」可选项，**默认不勾选**；开始菜单快捷方式照常创建，不影响使用。

## v1.3.1（2026-09-18）

### 修复

- **新增模型时，正在使用的模型像是「被清空了」**：点「+ 添加」会立刻新建一个条目、把编辑区切到它、并且把它标记为「使用中」——原来的模型在界面上一下"消失"了，看起来就像配置被清空。现在**新增模型不再顶掉正在使用的模型**：新条目只是打开成一个待填写的草稿（列表里标「待填写」），「● 使用中」始终留在原来那个可用的模型上；填好并保存后，想看效果再点「设为使用」。
- **列表里堆积空条目**：只有名字、接口 / Key / 模型名全空的草稿条目会被写进数据文件，越加越多。现在保存时会自动丢弃这类空条目，启动时也会顺手清理掉已有的空条目（至少保留一条）。
- **设置弹窗勾选项文字挤成一列并重叠**：勾选项误用了章节列表里 18×18 的方形勾选按钮样式，导致「所有模型都未配置 Key 时…」这行字被压成竖排、和下方的说明文字糊在一起。现在换成设置弹窗专用的勾选样式，文字正常单行显示。
- 清理了旧版单配置遗留的顶层 `temperature / maxTokens / extraBody` 字段，避免和新结构混淆。

## v1.3.0（2026-09-18）

### 新增

- **支持接入多个自定义 AI**：设置里的「模型设置」从「单个配置」升级为「模型列表」——左侧是已接入的模型，右侧是单个模型的接口地址 / Key / 模型名 / 推理参数，可同时接入多个（如「我的智谱」「公司私有模型」），随时在列表里点「设为使用」切换，保存后立即生效。每个模型自带独立的接口地址、Key、模型名与温度 / 最大输出 / 高级参数。
- **旧数据自动迁移**：此前只存一个自定义 AI 的配置（顶层 endpoint / apiKey / model）会在首次启动时自动迁移进新的「模型列表」第一项，原数据不丢失。
- **逐个模型「测试连接」**：在设置弹窗里点「测试当前编辑的模型连接」，会针对**当前正在编辑的那个**模型做连通性探测（而不是只能测「当前使用中的」），结果直接显示模型名和延迟，方便确认每个自定义 AI 是否可用。

## v1.2.2（2026-09-17）

### 新增

- **对话消息一键复制**：你的每条提问和助手的每条回答，鼠标移上去都会出现「复制」按钮，点一下即可复制**原文**（不是渲染后的格式，贴到别处不会带上多余的换行和符号）。
- **快捷键 Ctrl / Cmd + Shift + C**：直接复制最后一条回答；如果助手正在生成，则复制已经生成的部分。助手面板收起时不响应，不影响其它界面。
- 复制成功时按钮会短暂变成「已复制」，并弹出提示告诉复制了什么（"我的消息已复制" / "助手回答已复制"）。

## v1.2.1（2026-09-17）

### 修复

- **助手"只说不做"（重要）**：有一类模型不支持函数调用（实测 deepseek 系），它不会返回标准的工具调用，而是在正文里写 ```json 操作块```。此前只有"接口明确报错"时才会切到文本协议，于是最常见的情况——**接口收下了工具参数、模型却根本不会用**——触发了最坏结果：助手明明写好了"去读这几章"，系统一条都没执行，还把 JSON 原文当成回答丢给作者。现在**任何一轮只要出现合法操作块就立即执行**，并自动切换到文本协议（界面会提示已切换）。操作前也会做工具名白名单校验，避免把随口写的 json 当成操作。
- **界面满屏 `<br>`**：Markdown 渲染先拼 `<br>` 再整体转义，换行字面量 `<br>` 被直接显示出来；同时它不认识 ``` 代码块，助手的操作指令裸露成一堆乱码段落。现在代码块正常渲染成代码框，换行也正确了。
- **助手做完事却没有总结**：模型把操作执行完却没写结语时，以前只回一句"模型未输出"。现在会明确告知"已执行 N 项操作"，并给出一句可以接着问的话。

### 改进

- **只读操作也看得见**：读章节、读素材、重新拉取上下文都会在回答过程中显示（如"读了章节《演示章节》（2214 字）"），不用再猜它到底读没读。
- **长任务不会被中途截断**："把这条副线的所有章节读完再整合"这种活，文本协议模型是一批一批来的，轮次上限从 8 轮放宽到 16 轮。

## v1.2.0（2026-09-17）

### 修复

- **备忘「置顶」点了没反应**：底层 `patch` 只改了服务端数据、从不让界面重绘，所以点☆后星星不变、卡片也不上浮。现在所有修改都会刷新界面（边打字边存的场景仍保持静默，不打断光标）。同一个原因导致的**伏笔「标记已回收」点了没反应**、伏笔/设定/线路/备忘的表单保存后不刷新，也一并修好了。
- **AI 助手回答完就发不出消息**：点过「停止」后按钮停在被禁用状态，现在会正确复原。
- **AI 助手容易断**：流式连接每 10 秒发一次心跳，避免长时间只思考不产出时被中间层掐断连接。

### 新增

- **AI 助手可中断**：回答过程中发送按钮变为「■ 停止」，随时可以中止；已经生成的部分和已经执行的操作都会保留下来，不会白等。
- **思考过程可见**：推理模型（GLM、DeepSeek 等）的思考过程会实时流式展示，折叠块上标注思考耗时；回答结束后仍可展开回看（思考过程与耗时一并存入本地对话记录）。
- **流式回答**：回答逐字出现，不再是一整段突然蹦出来。
- **作品改名**：顶栏新增「✎ 改名」（也可直接改总览页的作品名输入框），下拉框会同步新名字。
- **大纲节点改名**：大纲树里双击标题即可就地改名（行内 ✎ 按钮同样可以）。

## v1.1.1（2026-09-16）

### 修复

- **AI 助手"读取没人应答"**：模型不支持 function calling 时的 JSON 文本协议里，工具清单漏了 `read_chapter` / `read_material`——助手请求读正文时被回"未知工具"，进而误判为自己"没有权限访问"。清单已补全，读取类工具真正可用。

### 新增

- **删除作品**：顶栏新增「🗑 删除」按钮，可删除当前误建/测试的作品。删除前会显示章节数与字数，并要求手动输入作品名确认；执行后连带清理章节、大纲、角色、关系、线路、节拍、伏笔、设定、备忘、素材等全部关联数据。

## v1.1.0（2026-09-16）

### 修复

- **安装版 AI 读不到项目数据**：新增 `read_chapter` 工具（支持按章节 ID / 标题模糊 / 序号读取正文全文，单次上限 150 章）；作者当前打开的那一章，全文会直接进入助手上下文。安装版不再出现"演示模式"误报。
- **问题被"吞"**：发送后用户消息立即显示在对话里并带等待计时；即使模型调用失败，服务端也返回一条可见的错误回复并落盘——你的问题不会再悄悄消失。
- **「演示模式」徽章不刷新**：之前只在启动时设置一次，现在设置变更后实时刷新。

## v1.0.0（2026-09-16）

### 首个公开版本

- 九个模块（总览 / 大纲 / 角色集 / 线路 / 章节 / 设定 / 伏笔 / 备忘 / 素材箱）通过"关联"串成一体
- AI 助手 15 个工具，可直接改写大纲、补节拍、建角色；原生 function calling / JSON 文本协议 / 演示模式三重降级
- 小说 TXT 批量导入（最多 20 个）：自动分章，可各自归档或合并为一本（每文件一卷），逐本进度 + 失败重试
- Windows 桌面安装包：目标电脑无需 Node、无需浏览器，数据全部留在本机
